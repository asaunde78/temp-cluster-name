apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: test-route
  namespace: kube-system
spec:
  parentRefs:
  - name: prod-web
    namespace: kube-system
  rules:
  - backendRefs:
    - name: hubble-ui
      namespace: kube-system
      port: 80
  hostnames:
    - hubble.internal.zinkithink.com