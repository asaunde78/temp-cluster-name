apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: foo
  namespace: kube-system
spec:
  parentRefs:
  - name: prod-web
  rules:
  - backendRefs:
    - name: vault-ui
      namespace: kube-system
      port: 8200